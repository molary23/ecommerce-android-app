<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mattire" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/light_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="titleCentered">true</item>
        <item name="actionMenuTextColor">@color/white</item>
       <!-- <item name="android:textColorPrimary">@color/white</item>-->
        <item name="android:statusBarColor">?attr/colorPrimaryDark</item>
    </style>

    <style name="Theme.Mattire" parent="Base.Theme.Mattire" />

    <style name="MyActionBar" parent="@style/Widget.Material3.ActionBar.Solid">
        <item name="android:background">@color/blue</item>
        <item name="background">@color/blue</item>
    </style>
</resources>